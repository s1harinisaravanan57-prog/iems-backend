# Server
PORT=3000
NODE_ENV=production

# PostgreSQL (use Render's database URL or your own)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iems_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# JWT
JWT_SECRET=your_very_secure_jwt_secret_change_in_production
JWT_EXPIRES_IN=7d
